// Aguarda o carregamento completo do DOM antes de executar o código
document.addEventListener("DOMContentLoaded", function () {

    // Altera o cursor padrão do corpo da página para uma imagem personalizada
    document.body.style.cursor = "url('Assets/Outros/cursor-pixel.png') 0 0, auto";
    // '0 0' define o ponto ativo do cursor (hotspot) no canto superior esquerdo da imagem
    // 'auto' é o cursor alternativo caso a imagem não carregue corretamente

});


